# 选择性同步主项目 - 使用说明

## 📋 功能介绍

这个工具让您可以**精确控制**从主项目获取哪些更新，而不会影响您的自定义功能。

### **核心优势**

- ✅ **保留所有自定义功能** - 不会丢失任何您的修改
- ✅ **精确控制更新** - 只获取需要的提交
- ✅ **避免大规模冲突** - 不需要一次性合并所有更新
- ✅ **安全可靠** - 每次只应用选定的提交

---

## 🚀 快速开始

### **步骤1: 启动工具**

双击运行：`选择性同步主项目.bat`

### **步骤2: 选择操作模式**

脚本提供5种操作模式：

#### **模式1: 应用特定提交** ⭐ 最精确
- 查看提交列表
- 输入想要的提交hash
- 查看详细修改内容
- 确认后应用

**适用场景**：获取主项目的某个特定bug修复或功能

#### **模式2: 应用最近N个提交**
- 指定数量（如5个）
- 一次性应用多个提交
- 按时间顺序应用

**适用场景**：获取最近的一批更新

#### **模式3: 应用所有新提交** ⚠️ 谨慎使用
- 一次性合并所有更新
- 等同于 `git merge origin/main`
- 可能产生较多冲突

**适用场景**：确定要完全同步主项目时

#### **模式4: 查看提交详情**
- 输入提交hash
- 查看完整的修改内容
- 不执行应用操作

**适用场景**：了解某个提交的具体内容

#### **模式5: 退出**

---

## 💡 推荐工作流程

### **每周定期检查**

```bash
# 1. 启动工具
双击: 选择性同步主项目.bat

# 2. 查看新提交
选择选项1，查看主项目的新提交列表

# 3. 评估是否需要
- Bug修复 → 建议应用
- 新功能 → 根据需求决定
- 破坏性更新 → 谨慎评估

# 4. 应用选定的提交
输入提交hash，确认应用

# 5. 推送到私人仓库
脚本会询问是否推送，选择 y
```

### **获取特定功能**

假设您看到主项目有个有用的更新：

1. **查看提交列表**
   ```
   c4d1949 - 调整检测框面积过滤阈值
   ba5eea9 - 优化翻译导入功能
   ```

2. **选择模式1**（应用特定提交）

3. **输入hash**
   ```
   Commit hash: ba5eea9
   ```

4. **查看详情并确认**
   ```
   Continue? (y/n): y
   ```

5. **推送到私人仓库**
   ```
   Push to myfork/don? (y/n): y
   ```

---

## 🛡️ 冲突处理

如果应用提交时遇到冲突：

### **步骤1: 识别冲突**

脚本会显示：
```
[ERROR] Cherry-pick failed - conflicts detected
```

### **步骤2: 查看冲突文件**

```bash
git status
```

### **步骤3: 手动解决冲突**

编辑冲突文件，找到冲突标记：

```
<<<<<<< HEAD
您的代码（自定义功能）
=======
主项目的代码（新更新）
>>>>>>>
```

**决策原则**：
- 如果是您的自定义功能 → 保留您的版本
- 如果是bug修复 → 保留主项目版本
- 如果两者都重要 → 手动合并

### **步骤4: 完成解决**

```bash
# 标记已解决
git add .

# 继续cherry-pick
git cherry-pick --continue

# 推送更新
git push myfork don
```

### **步骤5: 如果放弃**

```bash
# 中止cherry-pick
git cherry-pick --abort
```

---

## 📊 示例场景

### **场景1: 获取bug修复**

主项目修复了一个OCR识别bug：

```bash
# 1. 启动工具，查看提交
c51c08b - 修复正则表达式无法匹配translated字段的问题

# 2. 选择模式1，输入hash
Commit hash: c51c08b

# 3. 确认应用
Continue? (y/n): y

# 4. 推送
Push to myfork/don? (y/n): y
```

**结果**：获得bug修复，保留所有自定义功能 ✅

### **场景2: 获取性能优化**

主项目优化了翻译速度：

```bash
# 1. 查看最近10个提交
选择选项2: 应用最近N个提交

# 2. 输入数量
Number of commits: 3

# 3. 预览将应用的提交
Will apply these commits (oldest first):
  ba5eea9 - 优化翻译导入功能
  a03c920 - 修复收藏功能
  069ca98 - 移除调试日志

# 4. 确认应用
Continue? (y/n): y
```

**结果**：批量获取优化更新 ✅

### **场景3: 只查看不应用**

想了解某个提交的详细内容：

```bash
# 1. 选择模式4: 查看提交详情
Enter commit hash: c4d1949

# 2. 查看完整的修改内容
# （显示文件diff）

# 3. 评估后决定是否需要
# 如果需要，重新运行选择模式1应用
```

---

## ⚠️ 注意事项

### **DO（推荐做法）**

- ✅ 定期查看主项目更新
- ✅ 优先应用bug修复
- ✅ 每次只应用少量提交
- ✅ 应用前查看提交详情
- ✅ 应用后及时推送到私人仓库

### **DON'T（避免操作）**

- ❌ 一次性应用所有提交（除非确定需要）
- ❌ 不看提交内容就盲目应用
- ❌ 冲突时强制覆盖（仔细评估）
- ❌ 忘记推送到私人仓库

---

## 🔧 高级用法

### **查看更多提交**

默认显示20个提交，如需查看更多：

```bash
# 方法1: 使用批处理菜单
选择性同步主项目.bat → 选项2 (查看50个提交)

# 方法2: 直接运行PowerShell
powershell -File 选择性同步主项目.ps1 -Count 50
```

### **手动Cherry-pick**

如果您熟悉Git命令，也可以手动操作：

```bash
# 查看提交
git log origin/main --oneline -20

# 应用特定提交
git cherry-pick <hash>

# 推送
git push myfork don
```

---

## 📝 常见问题

### **Q: 会丢失我的自定义功能吗？**
A: 不会。Cherry-pick只添加选定的提交，不会删除您的修改。

### **Q: 如果冲突了怎么办？**
A: 手动解决冲突，选择保留哪个版本。详见"冲突处理"章节。

### **Q: 可以撤销应用的提交吗？**
A: 可以。使用 `git revert <hash>` 或 `git reset --hard HEAD~1`

### **Q: 多久同步一次？**
A: 建议每周检查一次主项目更新，按需应用。

### **Q: 如何知道哪些提交值得应用？**
A: 
- Bug修复：通常值得应用
- 新功能：根据需求决定
- 破坏性更改：谨慎评估
- 性能优化：推荐应用

---

## 🎯 总结

使用选择性同步工具，您可以：

1. **完全控制**更新内容
2. **保留所有**自定义功能
3. **避免冲突**风险
4. **灵活获取**主项目的优质更新

这是在保持自定义功能的同时获取主项目更新的**最佳实践**！

---

**祝使用愉快！** 🎉
