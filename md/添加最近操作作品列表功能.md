# 添加最近操作作品列表功能

## 🎯 功能描述

在高级文件夹的左侧面板添加"最近操作"列表，显示用户最近选择过的作品，方便快速访问。

---

## 📸 界面效果

### 左侧面板布局

```
搜索作品:
[输入或选择...        ]
[  刷新映射名称        ]

┌─────────────────────┐
│ 最近操作:           │  ← 新增区域
├─────────────────────┤
│ 作品1               │  ← 显示4行
│ 作品2               │
│ 作品3               │
│ 作品4               │
└─────────────────────┘

快速操作:
[⚡ 智能选择 ▼]
[全选章节]
[取消全选]
```

---

## ✨ 功能特点

### 1. 自动记录
- ✅ 点击"确定"时自动保存选中的作品
- ✅ 最多保存10个作品（显示最近4个）
- ✅ 新操作的作品排在最前面
- ✅ 自动去重

### 2. 快速跳转
- ✅ 双击作品名称
- ✅ 自动在右侧列表中定位
- ✅ 自动展开该作品
- ✅ 滚动到可见区域

### 3. 持久化存储
- ✅ 使用 QSettings 保存
- ✅ 关闭程序后保留
- ✅ 下次打开自动加载

---

## 🔧 技术实现

### 1. 创建最近作品列表

```python
# 创建最近作品列表
self.recent_works_list = QTreeWidget()
self.recent_works_list.setHeaderHidden(True)  # 隐藏表头
self.recent_works_list.setMaximumHeight(100)  # 显示4行左右
self.recent_works_list.setRootIsDecorated(False)  # 不显示展开箭头
self.recent_works_list.itemDoubleClicked.connect(self.on_recent_work_double_clicked)
layout.addWidget(self.recent_works_list)

# 加载最近操作的作品
self._load_recent_works()
```

### 2. 加载最近作品

```python
def _load_recent_works(self):
    """加载最近操作的作品"""
    try:
        import json
        settings = QSettings("MangaTranslator", "AdvancedFolder")
        recent_data = settings.value("recent_works", "")
        
        if not recent_data:
            return
        
        recent_works = json.loads(recent_data)
        self.recent_works_list.clear()
        
        # 显示最近4个作品
        for work_name in recent_works[:4]:
            item = QTreeWidgetItem(self.recent_works_list)
            item.setText(0, work_name)
            item.setData(0, Qt.ItemDataRole.UserRole, work_name)
            
    except Exception as e:
        self._log(f"⚠️ 加载最近作品失败: {e}")
```

### 3. 保存最近作品

```python
def _save_recent_works(self):
    """保存最近操作的作品"""
    try:
        import json
        settings = QSettings("MangaTranslator", "AdvancedFolder")
        
        # 获取当前选中的作品
        selected_works = set()
        root = self.title_tree.invisibleRootItem()
        for i in range(root.childCount()):
            work_item = root.child(i)
            # 检查是否有子项被选中
            if self._has_checked_children(work_item):
                work_name = work_item.text(0)
                selected_works.add(work_name)
        
        if not selected_works:
            return
        
        # 加载现有的最近作品
        recent_data = settings.value("recent_works", "")
        if recent_data:
            recent_works = json.loads(recent_data)
        else:
            recent_works = []
        
        # 将新选中的作品添加到列表开头，移除重复
        for work in selected_works:
            if work in recent_works:
                recent_works.remove(work)
            recent_works.insert(0, work)
        
        # 只保留最近10个
        recent_works = recent_works[:10]
        
        # 保存
        settings.setValue("recent_works", json.dumps(recent_works, ensure_ascii=False))
        self._log(f"✓ 已保存最近操作: {len(selected_works)} 个作品")
        
    except Exception as e:
        self._log(f"⚠️ 保存最近作品失败: {e}")
```

### 4. 检查是否有选中的子项

```python
def _has_checked_children(self, item: QTreeWidgetItem) -> bool:
    """检查项目是否有被选中的子项"""
    for i in range(item.childCount()):
        child = item.child(i)
        if child.checkState(0) == Qt.CheckState.Checked:
            return True
        if self._has_checked_children(child):
            return True
    return False
```

### 5. 双击跳转

```python
def on_recent_work_double_clicked(self, item: QTreeWidgetItem, column: int):
    """双击最近作品时跳转到该作品"""
    work_name = item.data(0, Qt.ItemDataRole.UserRole)
    if not work_name:
        return
    
    # 在作品列表中查找并展开
    root = self.title_tree.invisibleRootItem()
    for i in range(root.childCount()):
        work_item = root.child(i)
        if work_item.text(0) == work_name:
            # 清除其他选中
            self.title_tree.clearSelection()
            # 选中并展开
            work_item.setSelected(True)
            work_item.setExpanded(True)
            # 滚动到可见区域
            self.title_tree.scrollToItem(work_item)
            self._log(f"✓ 已跳转到: {work_name}")
            break
```

---

## 📊 数据存储

### QSettings 配置

| 键名 | 值类型 | 说明 |
|------|--------|------|
| `recent_works` | JSON字符串 | 最近操作的作品列表 |

### 数据格式

```json
[
  "作品名1",
  "作品名2",
  "作品名3",
  ...
]
```

### 存储规则

- **最多保存**：10个作品
- **显示数量**：4个作品
- **排序规则**：最新的在最前面
- **去重规则**：同一作品只保留最新的记录

---

## 🎮 使用场景

### 场景1：快速访问常用作品

**需求**：经常翻译某几部漫画

**操作流程：**
1. 第一次选择作品并翻译
2. 下次打开高级文件夹
3. 在"最近操作"列表中看到该作品
4. 双击作品名称
5. ✅ 自动跳转并展开

### 场景2：继续上次的工作

**需求**：上次翻译了一半，想继续

**操作流程：**
1. 打开高级文件夹
2. 查看"最近操作"列表
3. 看到上次操作的作品
4. 双击跳转
5. ✅ 快速定位到上次的作品

### 场景3：批量处理多个作品

**需求**：今天翻译了5部漫画

**操作流程：**
1. 每次翻译后自动记录
2. "最近操作"显示最近4部
3. 完整记录保存最近10部
4. ✅ 方便查看今天的工作内容

---

## 💡 设计理念

### 1. 自动化

**原则**：无需用户手动操作

**实现**：
- 自动记录选中的作品
- 自动保存到配置
- 自动加载历史记录

### 2. 智能去重

**问题**：同一作品可能多次操作

**解决**：
- 检测重复作品
- 移除旧记录
- 将新记录放在最前面

### 3. 空间优化

**问题**：左侧面板空间有限

**解决**：
- 只显示4个作品
- 高度限制100px
- 完整记录保存10个

---

## 🔄 工作流程

```
用户选择章节并点击"确定"
    ↓
accept() 方法
    ↓
_save_recent_works()
    ↓
遍历所有作品项
    ↓
检查是否有选中的章节
    ↓
收集选中的作品名称
    ↓
加载现有的最近作品列表
    ↓
┌──────────────────────────┐
│ 1. 移除重复的作品        │
│ 2. 将新作品插入到开头    │
│ 3. 只保留最近10个        │
│ 4. 保存到 QSettings      │
└──────────────────────────┘
    ↓
下次打开时自动加载显示
```

---

## 📝 修改的文件

**文件**：`desktop_qt_ui/widgets/advanced_folder_panel.py`

**修改位置：**

1. **左侧面板创建**（第169-181行）
   - 添加"最近操作"标签
   - 创建 `recent_works_list` 控件
   - 调用 `_load_recent_works()`

2. **accept方法**（第1193-1198行）
   - 添加 `_save_recent_works()` 调用

3. **新增方法**（第1216-1309行）
   - `_load_recent_works()` - 加载最近作品
   - `_save_recent_works()` - 保存最近作品
   - `_has_checked_children()` - 检查是否有选中子项
   - `on_recent_work_double_clicked()` - 双击跳转

**代码统计：**
- 新增：约100行
- 修改：3行

---

## ✅ 测试验证

### 测试1：自动记录

1. 选择几个作品的章节
2. 点击"确定"
3. 重新打开高级文件夹
4. **预期**：在"最近操作"中看到这些作品 ✅

### 测试2：双击跳转

1. 在"最近操作"中双击一个作品
2. **预期**：
   - 右侧列表自动定位到该作品 ✅
   - 作品自动展开 ✅
   - 滚动到可见区域 ✅

### 测试3：去重功能

1. 第一次选择"作品A"
2. 第二次再选择"作品A"
3. **预期**："作品A"只出现一次，在最前面 ✅

### 测试4：数量限制

1. 连续操作12个不同的作品
2. **预期**：
   - 显示最近4个 ✅
   - 保存最近10个 ✅
   - 最早的2个被移除 ✅

---

## 🎉 总结

| 项目 | 内容 |
|------|------|
| **功能** | 最近操作作品列表 |
| **显示** | 4行，100px高度 |
| **存储** | 最多10个作品 |
| **交互** | 双击跳转 |
| **自动化** | 自动记录、加载 |

---

✅ **现在可以在"最近操作"区域快速访问最近选择过的作品了！**

## 💬 用户反馈响应

**用户需求**：这个区域加个保留上个操作所选的作品，列4行

- ✅ 已实现：添加"最近操作"列表
- ✅ 已实现：显示4行（高度100px）
- ✅ 已实现：自动保存选中的作品
- ✅ 已实现：双击跳转功能

感谢您的建议，让功能更加便捷！
