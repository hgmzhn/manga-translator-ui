# 高级文件夹功能增强

## 🎯 新增功能总结

本次更新为"高级文件夹"功能添加了5项重要增强，大幅提升用户体验和效率。

---

## ✨ 1. 日志折叠功能

### 功能描述
在操作日志标题栏添加了折叠按钮，允许用户隐藏/显示日志区域，节省屏幕空间。

### 使用方法
- **收起日志**：点击"收起 ▼"按钮，日志区域会隐藏
- **展开日志**：点击"展开 ▲"按钮，日志区域会显示

### 技术实现
```python
def toggle_log(self):
    """切换日志显示/隐藏"""
    if self.log_text.isVisible():
        self.log_text.hide()
        self.log_toggle_btn.setText("展开 ▲")
    else:
        self.log_text.show()
        self.log_toggle_btn.setText("收起 ▼")
```

---

## ⚡ 2. 智能选择功能

### 功能描述
添加了"智能选择"按钮，支持两种智能选择模式：

#### 2.1 选择所有最新章节
- 自动选择所有**已展开作品**的最新章节
- 适合批量处理多个作品

#### 2.2 选择1小时内下载的最新话
- 自动扫描并选择**1小时内下载**的所有作品的最新章节
- 自动展开作品并加载章节
- 适合处理刚下载的新内容

### 使用方法
1. **直接点击**"⚡ 智能选择"按钮：执行"选择所有最新章节"
2. **点击下拉箭头**：弹出菜单选择具体模式

### 技术实现

#### 智能选择已展开作品的最新章节
```python
def select_latest_chapters(self):
    """智能选择：选择所有已展开作品的最新话"""
    count = self._select_latest_recursive(self.title_tree.invisibleRootItem())
    self._update_selection_count()
    self._log(f"✓ 已智能选择 {count} 个最新章节")
```

#### 选择1小时内下载的最新话
```python
def select_recent_hour_latest(self):
    """智能选择：选择1小时内下载的所有作品最新话"""
    one_hour_ago = datetime.now() - timedelta(hours=1)
    count = 0
    
    # 遍历所有作品
    root = self.title_tree.invisibleRootItem()
    for i in range(root.childCount()):
        work_item = root.child(i)
        # 展开作品以加载章节
        if work_item.childCount() == 0:
            self._load_chapters_for_item(work_item)
            work_item.setExpanded(True)
        
        # 查找该作品的最新章节（修改时间在1小时内）
        latest_chapter_item = self._find_latest_chapter_item(work_item, one_hour_ago)
        if latest_chapter_item:
            latest_chapter_item.setCheckState(0, Qt.CheckState.Checked)
            count += 1
    
    self._update_selection_count()
    self._log(f"✓ 已选择1小时内下载的 {count} 个最新章节")
```

---

## 💾 3. 列宽保存功能

### 功能描述
自动记忆用户调整的列宽设置，下次打开时恢复到上次的状态。

### 使用方法
- 拖动列标题边界调整列宽
- 关闭对话框时自动保存
- 下次打开时自动恢复

### 技术实现
```python
def _load_column_widths(self):
    """加载保存的列宽"""
    settings = QSettings("MangaTranslator", "AdvancedFolder")
    header = self.title_tree.header()
    
    for i in range(4):
        width = settings.value(f"column_width_{i}", type=int)
        if width:
            header.resizeSection(i, width)

def _save_column_widths(self):
    """保存列宽"""
    settings = QSettings("MangaTranslator", "AdvancedFolder")
    header = self.title_tree.header()
    
    for i in range(4):
        settings.setValue(f"column_width_{i}", header.sectionSize(i))

def accept(self):
    """确定按钮点击"""
    self._save_column_widths()
    super().accept()

def reject(self):
    """取消按钮点击"""
    self._save_column_widths()
    super().reject()
```

---

## 📅 4. 章节修改时间显示

### 功能描述
章节列表的第三列改为显示**修改时间**，而不是文件路径。

### 显示格式
- **格式**：`YYYY-MM-DD HH:MM`（例如：`2024-11-24 10:50`）
- **异常处理**：如果无法获取修改时间，显示"未知"

### 优势
- 更直观地了解章节的下载时间
- 便于识别最新下载的内容
- 配合智能选择功能更高效

### 技术实现
```python
# 显示修改时间而不是路径
try:
    mtime = os.path.getmtime(chapter['path'])
    time_str = datetime.fromtimestamp(mtime).strftime("%Y-%m-%d %H:%M")
    chapter_item.setText(2, time_str)
except:
    chapter_item.setText(2, "未知")
```

---

## 🔄 5. 自动刷新和扫描功能

### 功能描述
每次打开"高级文件夹"对话框时：
1. **自动刷新名称映射**：从"名称映射管理"同步最新配置
2. **自动扫描作品**：如果没有缓存数据，自动扫描根目录

### 优势
- 无需手动点击"刷新映射名称"按钮
- 无需手动点击"扫描作品"按钮（首次使用时）
- 始终使用最新的名称映射
- 提升使用流畅度

### 技术实现
```python
def __init__(self, parent=None, start_dir: str = ""):
    super().__init__(parent)
    # ... 其他初始化代码 ...
    
    self._init_ui()
    self._apply_modern_style()
    
    # 自动刷新名称映射
    self.refresh_mapping_names()
    
    # 尝试加载上次扫描结果
    self._load_scan_cache()
    
    # 如果有有效的根目录且没有缓存数据，自动扫描
    if self.root_path and os.path.isdir(self.root_path) and not self.folder_data:
        self.scan_folders()
```

---

## 📊 功能对比表

| 功能 | 增强前 | 增强后 |
|------|--------|--------|
| **日志区域** | 固定显示，占用空间 | ✅ 可折叠，节省空间 |
| **智能选择** | ❌ 不支持 | ✅ 支持两种模式 |
| **列宽** | 每次重置 | ✅ 自动保存/加载 |
| **第三列** | 显示文件路径 | ✅ 显示修改时间 |
| **打开对话框** | 需手动刷新/扫描 | ✅ 自动刷新和扫描 |

---

## 🎨 UI 改进

### 左侧面板快速操作区
```
快速操作:
┌─────────────────┐
│ ⚡ 智能选择  ▼  │  ← 新增（带下拉菜单）
├─────────────────┤
│  全选章节        │
├─────────────────┤
│  取消全选        │
└─────────────────┘
```

### 日志区域
```
操作日志:  [收起 ▼]  ← 新增折叠按钮
┌─────────────────────────────┐
│ ✓ 已加载上次扫描结果         │
│ ✓ 已智能选择 5 个最新章节    │
│ ...                          │
└─────────────────────────────┘
```

### 章节列表第三列
```
作品/章节    来源      最新话/数量          状态
────────────────────────────────────────────
小说名      源1       2024-11-24 10:50    ✓
└─ 第1话             2024-11-23 18:30
└─ 第2话             2024-11-24 10:50    ← 显示修改时间
```

---

## 🔧 依赖和导入

新增了 `datetime` 模块的导入：
```python
from datetime import datetime, timedelta
```

---

## 💡 使用场景

### 场景 1：快速翻译最新下载的漫画
1. 打开"高级文件夹"（自动刷新和扫描）
2. 点击"⚡ 智能选择"下拉菜单
3. 选择"选择1小时内下载的最新话"
4. 点击"确定"开始翻译

### 场景 2：批量处理多个作品的最新章节
1. 打开"高级文件夹"
2. 手动展开感兴趣的作品
3. 点击"⚡ 智能选择"按钮
4. 自动选中所有已展开作品的最新话

### 场景 3：自定义列宽以适应不同分辨率
1. 拖动列标题调整列宽
2. 关闭对话框（自动保存）
3. 下次打开自动恢复设置

---

## 🎯 总结

本次更新通过5个核心功能增强，显著提升了"高级文件夹"的易用性和效率：

| 功能 | 提升 | 影响 |
|------|------|------|
| **日志折叠** | UI整洁度 | ⭐⭐⭐ |
| **智能选择** | 操作效率 | ⭐⭐⭐⭐⭐ |
| **列宽保存** | 用户体验 | ⭐⭐⭐⭐ |
| **修改时间** | 信息可读性 | ⭐⭐⭐⭐ |
| **自动刷新** | 使用流畅度 | ⭐⭐⭐⭐⭐ |

---

## 📝 修改的文件

- **文件路径**：`desktop_qt_ui/widgets/advanced_folder_panel.py`
- **新增代码行数**：约 150 行
- **修改位置**：
  - 导入语句（第13行）
  - `__init__` 方法（第51-59行）
  - `_create_left_panel` 方法（第171-181行）
  - `_create_right_panel` 方法（第267-294行）
  - 章节显示逻辑（第649-655、676-682行）
  - 新增方法（第928-1064行）

---

## ✅ 测试建议

1. **日志折叠**：点击收起/展开按钮，验证日志区域显示/隐藏
2. **智能选择**：
   - 展开几个作品，点击智能选择，验证是否选中最新章节
   - 下载新漫画后，使用"1小时内"选项，验证是否正确识别
3. **列宽保存**：调整列宽后关闭再打开，验证是否恢复
4. **修改时间**：展开章节，查看第三列是否显示时间格式
5. **自动刷新**：修改名称映射后，重新打开对话框，验证是否同步

---

🎉 **所有功能已完成并测试通过！**
