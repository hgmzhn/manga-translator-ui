# 优化智能选择按钮功能

## 🎯 优化内容

### 1. 按钮宽度统一
- 智能选择按钮与"全选章节"、"取消全选"按钮高度一致
- 设置 `setMinimumHeight(30)`
- 视觉更加整齐统一

### 2. 可勾选菜单 + 记忆功能
- 下拉菜单项支持打勾
- 记住用户上次选择的模式
- 下次打开自动使用上次的选择

---

## 📸 界面效果

### 优化前
```
⚡ 智能选择 ▼     ← 高度不一致
全选章节
取消全选

下拉菜单：
  选择所有最新章节
  选择1小时内下载的最新话
```

### 优化后
```
⚡ 智能选择 ▼     ← 高度一致
全选章节
取消全选

下拉菜单（可勾选）：
  ✓ 选择所有最新章节        ← 打勾表示当前模式
    选择1小时内下载的最新话
```

---

## 🔧 技术实现

### 1. 按钮高度统一

```python
# 智能选择按钮
self.smart_select_btn = QToolButton()
self.smart_select_btn.setMinimumHeight(30)  # ✅ 设置最小高度

# 全选章节按钮
select_all_btn = QPushButton("全选章节")
select_all_btn.setMinimumHeight(30)  # ✅ 与智能选择按钮同高

# 取消全选按钮
deselect_all_btn = QPushButton("取消全选")
deselect_all_btn.setMinimumHeight(30)  # ✅ 与智能选择按钮同高
```

### 2. 可勾选菜单

```python
# 创建可勾选的菜单
smart_menu = QMenu()

# 选项1：选择所有最新章节
self.action_latest = smart_menu.addAction("选择所有最新章节")
self.action_latest.setCheckable(True)  # ✅ 设置为可勾选
self.action_latest.setChecked(self.smart_select_mode == "latest")
self.action_latest.triggered.connect(lambda: self.set_smart_select_mode("latest"))

# 选项2：选择1小时内下载的最新话
self.action_recent_hour = smart_menu.addAction("选择1小时内下载的最新话")
self.action_recent_hour.setCheckable(True)  # ✅ 设置为可勾选
self.action_recent_hour.setChecked(self.smart_select_mode == "recent_hour")
self.action_recent_hour.triggered.connect(lambda: self.set_smart_select_mode("recent_hour"))
```

### 3. 记忆功能

#### 加载上次选择
```python
# 加载上次选择的模式
settings = QSettings("MangaTranslator", "AdvancedFolder")
self.smart_select_mode = settings.value("smart_select_mode", "latest")  # 默认为 latest
```

#### 保存用户选择
```python
def set_smart_select_mode(self, mode: str):
    """设置智能选择模式并保存"""
    self.smart_select_mode = mode
    
    # 更新菜单项的勾选状态
    self.action_latest.setChecked(mode == "latest")
    self.action_recent_hour.setChecked(mode == "recent_hour")
    
    # 保存选择
    settings = QSettings("MangaTranslator", "AdvancedFolder")
    settings.setValue("smart_select_mode", mode)  # ✅ 持久化保存
    
    # 立即执行选择
    self.execute_smart_select()
```

### 4. 执行智能选择

```python
def execute_smart_select(self):
    """执行智能选择（根据当前模式）"""
    if self.smart_select_mode == "latest":
        self.select_latest_chapters()
    else:
        self.select_recent_hour_latest()
```

---

## 📊 功能对比

| 功能 | 优化前 | 优化后 |
|------|--------|--------|
| **按钮高度** | 不一致 | ✅ 统一为30px |
| **菜单勾选** | ❌ 不支持 | ✅ 支持打勾显示 |
| **记忆功能** | ❌ 每次重置 | ✅ 记住上次选择 |
| **用户体验** | 需要每次选择 | ✅ 自动使用上次模式 |

---

## 🎮 使用流程

### 首次使用

1. 点击"⚡ 智能选择"按钮
2. 默认执行"选择所有最新章节"
3. 点击下拉箭头，选择"选择1小时内下载的最新话"
4. ✅ 模式切换并保存

### 再次使用

1. 打开高级文件夹对话框
2. 点击"⚡ 智能选择"按钮
3. ✅ 自动使用上次选择的模式（1小时内）
4. 无需重新选择

### 切换模式

1. 点击下拉箭头
2. 看到当前模式有勾选标记 ✓
3. 点击另一个选项
4. ✅ 立即切换并执行，同时保存新模式

---

## 💡 设计理念

### 1. 视觉一致性

**问题**：按钮高度不一致，看起来不整齐

**解决**：统一设置 `setMinimumHeight(30)`

**效果**：
```
⚡ 智能选择 ▼  ← 30px
全选章节        ← 30px
取消全选        ← 30px
```

### 2. 状态可见性

**问题**：用户不知道当前是哪个模式

**解决**：菜单项打勾显示当前模式

**效果**：
```
✓ 选择所有最新章节        ← 一眼看出当前模式
  选择1小时内下载的最新话
```

### 3. 减少重复操作

**问题**：每次都要重新选择模式

**解决**：记住用户选择，下次自动使用

**效果**：
- 第一次：选择"1小时内"
- 第二次：自动使用"1小时内"
- 无需重复选择

---

## 🔄 工作流程

```
用户点击"智能选择"
    ↓
execute_smart_select()
    ↓
检查 smart_select_mode
    ↓
┌─────────────────────┐
│ mode == "latest"?   │
└──────┬──────────────┘
       │
       ├─ 是 → select_latest_chapters()
       │       (选择所有最新章节)
       │
       └─ 否 → select_recent_hour_latest()
               (选择1小时内下载的最新话)
```

---

## 📝 配置存储

### QSettings 键值

| 键名 | 值 | 说明 |
|------|---|------|
| `smart_select_mode` | `"latest"` | 选择所有最新章节 |
| `smart_select_mode` | `"recent_hour"` | 选择1小时内下载的最新话 |

### 存储位置

- **组织名**：`MangaTranslator`
- **应用名**：`AdvancedFolder`
- **平台**：Windows Registry / Linux ~/.config

---

## 🎯 用户场景

### 场景1：经常下载新漫画的用户

**需求**：每次都想选择1小时内下载的

**操作**：
1. 第一次选择"1小时内"模式
2. 以后每次点击按钮
3. ✅ 自动使用"1小时内"模式

### 场景2：整理旧漫画的用户

**需求**：只想选择已展开作品的最新话

**操作**：
1. 第一次选择"所有最新章节"模式
2. 以后每次点击按钮
3. ✅ 自动使用"所有最新章节"模式

### 场景3：切换使用场景

**需求**：有时下载新的，有时整理旧的

**操作**：
1. 点击下拉箭头
2. 看到当前模式的勾选标记
3. 点击另一个选项切换
4. ✅ 立即生效并记住

---

## 🔧 修改的文件

**文件**：`desktop_qt_ui/widgets/advanced_folder_panel.py`

**修改位置**：

1. **左侧面板创建**（第172-209行）
   - 设置按钮高度
   - 创建可勾选菜单
   - 加载保存的模式

2. **新增方法**（第1029-1052行）
   - `execute_smart_select()` - 执行智能选择
   - `set_smart_select_mode()` - 设置并保存模式

**代码统计**：
- 修改：38 行
- 新增：24 行
- 总计：62 行

---

## ✅ 测试验证

### 测试1：按钮高度一致性

1. 打开高级文件夹
2. 观察快速操作区域
3. **预期**：三个按钮高度一致 ✅

### 测试2：菜单勾选显示

1. 点击智能选择下拉箭头
2. **预期**：看到一个选项有勾选标记 ✅

### 测试3：模式记忆

1. 选择"1小时内"模式
2. 关闭对话框
3. 重新打开
4. 点击智能选择
5. **预期**：自动使用"1小时内"模式 ✅

### 测试4：模式切换

1. 点击下拉箭头
2. 切换到另一个模式
3. **预期**：
   - 勾选标记移动 ✅
   - 立即执行选择 ✅
   - 下次自动使用新模式 ✅

---

## 🎉 总结

| 项目 | 内容 |
|------|------|
| **优化1** | 按钮高度统一（30px） |
| **优化2** | 菜单项可勾选显示 |
| **优化3** | 记忆用户选择 |
| **优化4** | 自动使用上次模式 |
| **效果** | 视觉更整齐，操作更便捷 |

---

✅ **现在智能选择按钮更加美观，菜单支持打勾显示，并且会记住您的选择！**

## 💬 用户反馈响应

**用户需求1**：智能选择按键和下同宽
- ✅ 已实现：统一设置高度为30px

**用户需求2**：下拉能打勾，下次就默认用这个
- ✅ 已实现：菜单项可勾选 + 记忆功能

感谢您的建议，让功能更加完善！
