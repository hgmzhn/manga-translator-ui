# 修复扫描权限错误和根目录保存问题

## 🐛 问题描述

用户报告了两个问题：

1. **扫描失败**：`[WinError 5] 拒绝访问。: 'D:\\$RECYCLE.BIN\\S-1-5-18'`
2. **根目录仍未保存**：某些操作后根目录设置丢失

---

## 问题 1：扫描权限错误

### 🔍 原因分析

扫描代码尝试访问系统保护文件夹，如：
- `$RECYCLE.BIN`（回收站）
- `System Volume Information`（系统卷信息）
- 其他系统文件夹

这些文件夹需要管理员权限，普通用户无法访问。

### ✅ 修复方案

#### 1. 添加系统文件夹过滤列表
```python
# 系统文件夹和隐藏文件夹列表
system_folders = {'$RECYCLE.BIN', 'System Volume Information', 'Config.Msi', 
                'ProgramData', 'Windows', 'Program Files', 'Program Files (x86)',
                'PerfLogs', 'Recovery', '$Windows.~BT', '$Windows.~WS'}
```

#### 2. 跳过条件
```python
# 跳过系统文件夹、隐藏文件夹和以.开头的文件夹
if (not source_dir.is_dir() or 
    source_dir.name in system_folders or 
    source_dir.name.startswith('.') or    # 隐藏文件夹
    source_dir.name.startswith('$')):      # 系统文件夹
    continue
```

#### 3. 异常处理
```python
try:
    # 扫描逻辑
except PermissionError as e:
    self._log(f"⚠️ 跳过无权限访问的文件夹: {source_dir.name}")
    continue
except Exception as e:
    self._log(f"⚠️ 跳过错误文件夹 {source_dir.name}: {e}")
    continue
```

---

## 问题 2：根目录保存不完整

### 🔍 原因分析

之前只在关闭对话框时保存根目录，但遗漏了两个重要场景：
1. **浏览文件夹**后未保存
2. **扫描成功**后未保存

### ✅ 修复方案

#### 1. 浏览文件夹后立即保存
```python
def browse_folder(self):
    """浏览选择文件夹"""
    folder = QFileDialog.getExistingDirectory(
        self, "选择根目录",
        self.root_path_edit.text() or self.root_path
    )
    
    if folder:
        self.root_path_edit.setText(folder)
        self._log(f"已选择文件夹: {folder}")
        # ✅ 立即保存选择的根目录
        self._save_root_path()
```

#### 2. 扫描成功后保存
```python
def scan_folders(self):
    # ... 扫描逻辑 ...
    
    self._log(f"✓ 扫描完成！发现 {scanned_sources} 个来源，{len(self.folder_data)} 个作品")
    self.refresh_title_list()
    self.refresh_mapping_names()
    
    # ✅ 扫描成功后保存根目录
    self._save_root_path()
    
    # 保存扫描结果到缓存
    self._save_scan_cache()
```

---

## 📊 修复效果

### 扫描权限问题

| 文件夹类型 | 修复前 | 修复后 |
|------------|--------|--------|
| **普通文件夹** | ✅ 正常扫描 | ✅ 正常扫描 |
| **$RECYCLE.BIN** | ❌ 权限错误 | ✅ 自动跳过 |
| **System Volume Info** | ❌ 权限错误 | ✅ 自动跳过 |
| **.开头的隐藏文件夹** | ⚠️ 尝试扫描 | ✅ 自动跳过 |
| **$开头的系统文件夹** | ❌ 权限错误 | ✅ 自动跳过 |

### 根目录保存

| 操作场景 | 修复前 | 修复后 |
|----------|--------|--------|
| **浏览选择文件夹** | ❌ 不保存 | ✅ **立即保存** |
| **扫描成功** | ❌ 不保存 | ✅ **立即保存** |
| **确定/取消** | ✅ 保存 | ✅ 保存 |
| **修改文本框内容** | ❌ 不保存 | ❌ 不保存（需手动扫描） |

---

## 🎯 技术细节

### 跳过的系统文件夹列表

| 文件夹名 | 说明 |
|---------|------|
| **$RECYCLE.BIN** | Windows回收站 |
| **System Volume Information** | 系统还原点信息 |
| **Config.Msi** | Windows Installer缓存 |
| **ProgramData** | 程序数据（部分需要权限） |
| **Windows** | Windows系统文件夹 |
| **Program Files** | 程序安装目录 |
| **Program Files (x86)** | 32位程序目录 |
| **PerfLogs** | 性能日志 |
| **Recovery** | 恢复分区 |
| **$Windows.~BT** | Windows升级临时文件 |
| **$Windows.~WS** | Windows升级临时文件 |

### 过滤规则

1. **显式列表**：匹配 `system_folders` 集合
2. **前缀规则**：
   - `.` 开头 → Unix风格隐藏文件
   - `$` 开头 → Windows系统文件夹
3. **权限处理**：`PermissionError` → 跳过并记录日志

---

## 💡 使用建议

### 推荐的根目录位置

✅ **推荐**：
- `D:\漫画`
- `D:\Downloads`
- `C:\Users\用户名\Downloads`
- 任何用户创建的文件夹

❌ **避免**：
- `C:\` 或 `D:\` 根目录（包含大量系统文件夹）
- `C:\Windows`
- `C:\Program Files`
- 任何系统目录

### 最佳实践

1. **创建专用文件夹**
   ```
   D:\漫画\
   ├── 来源1\
   │   ├── 作品1\
   │   └── 作品2\
   └── 来源2\
       └── 作品3\
   ```

2. **避免根目录**
   - 不要直接选择盘符根目录（C:\、D:\）
   - 创建二级文件夹作为根目录

3. **权限问题**
   - 如需扫描特殊文件夹，以管理员身份运行程序
   - 但通常不需要扫描系统文件夹

---

## 📝 修改的代码

### 文件路径
`desktop_qt_ui/widgets/advanced_folder_panel.py`

### 修改内容

1. **browse_folder方法**（第462-473行）
   - 添加 `self._save_root_path()` 调用

2. **scan_folders方法**（第491-545行）
   - 添加系统文件夹过滤列表
   - 添加跳过条件判断
   - 添加异常处理
   - 添加扫描后保存根目录

### 新增代码行数
- +15 行（过滤逻辑）
- +8 行（异常处理）
- +2 行（保存调用）

---

## ✅ 测试验证

### 测试1：扫描系统盘根目录
1. 选择 `D:\` 作为根目录
2. 点击"扫描作品"
3. **预期**：跳过系统文件夹，无权限错误

### 测试2：浏览选择后关闭
1. 点击"浏览..."选择新文件夹
2. 直接关闭对话框
3. 重新打开
4. **预期**：根目录已保存

### 测试3：扫描后关闭
1. 输入根目录路径
2. 点击"扫描作品"
3. 扫描成功后关闭
4. 重新打开
5. **预期**：根目录已保存

---

## 🎉 总结

| 问题 | 修复方案 | 效果 |
|------|---------|------|
| **权限错误** | 过滤系统文件夹 + 异常处理 | ✅ 扫描稳定无错误 |
| **根目录丢失** | 多点保存策略 | ✅ 根目录始终记忆 |

---

✅ **现在扫描功能更加稳定，不会因为系统文件夹导致失败！**
✅ **根目录设置会在更多场景下被正确保存！**
