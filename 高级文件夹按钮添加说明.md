# 高级文件夹按钮添加说明

## ✅ 已完成的修改

### 修改1：主界面添加按钮（main_view.py）

**位置：** 第431行

**修改内容：**
```python
# 添加按钮定义
self.add_files_button = QPushButton("添加文件")
self.add_folder_button = QPushButton("添加文件夹")
self.advanced_folder_button = QPushButton("高级文件夹")  # ← 新增
self.clear_list_button = QPushButton("清空列表")

# 添加按钮到布局
file_buttons_layout.addWidget(self.add_files_button)
file_buttons_layout.addWidget(self.add_folder_button)
file_buttons_layout.addWidget(self.advanced_folder_button)  # ← 新增
file_buttons_layout.addWidget(self.clear_list_button)
```

---

### 修改2：连接按钮信号（main_view.py）

**位置：** 第483行

**修改内容：**
```python
self.add_files_button.clicked.connect(self._trigger_add_files)
self.add_folder_button.clicked.connect(self.controller.add_folder)
self.advanced_folder_button.clicked.connect(self.controller.open_advanced_folder)  # ← 新增
self.clear_list_button.clicked.connect(self.controller.clear_file_list)
```

---

### 修改3：添加处理方法（app_logic.py）

**位置：** 第574行

**修改内容：**
```python
def open_advanced_folder(self):
    """打开高级文件夹对话框，支持三层目录结构批量选择章节"""
    last_dir = self.get_last_open_dir()
    
    # 导入高级文件夹对话框
    from widgets.advanced_folder_panel import show_advanced_folder_dialog
    
    # 显示对话框并获取选中的章节路径
    selected_chapters = show_advanced_folder_dialog(
        parent=None,
        start_dir=last_dir
    )
    
    if selected_chapters:
        # 保存最后打开的目录（使用第一个章节的父目录）
        if selected_chapters:
            first_chapter_parent = os.path.dirname(selected_chapters[0])
            self.set_last_open_dir(first_chapter_parent)
        
        # 添加选中的章节到文件列表
        self.add_files(selected_chapters)
        
        self.logger.info(f"从高级文件夹对话框添加了 {len(selected_chapters)} 个章节")
```

---

## 🎨 最终效果

### 按钮布局

```
┌───────────────────────────────────────────────────┐
│ [添加文件] [添加文件夹] [高级文件夹] [清空列表]  │ ← 新增按钮
├───────────────────────────────────────────────────┤
│                                                   │
│              文件列表区域                          │
│                                                   │
└───────────────────────────────────────────────────┘
```

---

## 🚀 使用方法

### 步骤1：启动应用

```bash
python main.py
```

### 步骤2：点击"高级文件夹"按钮

点击主界面上的"高级文件夹"按钮

### 步骤3：使用高级功能

**对话框功能：**
1. **扫描三层目录** - Source/Title/Chapters结构
2. **智能聚合** - 自动合并同名作品
3. **来源排序** - 按最新话时间排序
4. **显示最新话** - 显示每个来源的最新章节
5. **自动展开** - 最新来源自动展开
6. **批量选择** - 支持Ctrl/Shift多选
7. **排序功能** - 智能/名称/日期排序
8. **搜索过滤** - 快速查找作品

### 步骤4：选择章节

1. 点击"扫描作品"按钮扫描目录
2. 展开作品查看来源和章节
3. 勾选需要翻译的章节
4. 点击"确定"按钮

### 步骤5：开始翻译

选中的章节会自动添加到文件列表，点击"开始翻译"即可！

---

## 📊 功能对比

| 功能 | 添加文件夹 | 高级文件夹 |
|------|-----------|-----------|
| **选择方式** | 系统文件浏览器 | 智能扫描+聚合 |
| **目录结构** | 任意 | 三层结构优化 |
| **多选** | ✅ 文件夹级别 | ✅ 章节级别 |
| **预览** | 文件夹列表 | 作品+来源+章节树 |
| **智能聚合** | ❌ | ✅ 同名作品合并 |
| **来源排序** | ❌ | ✅ 按最新话排序 |
| **搜索过滤** | 系统搜索 | ✅ 内置搜索 |
| **名称映射** | ❌ | ✅ 自动应用 |
| **缓存** | ❌ | ✅ 扫描结果缓存 |

---

## 💡 适用场景

### 使用"添加文件夹"的场景
- ✅ 单个或少量文件夹
- ✅ 简单目录结构
- ✅ 快速添加

### 使用"高级文件夹"的场景
- ✅ 大量文件夹批量管理
- ✅ 三层目录结构（Source/Title/Chapters）
- ✅ 多来源作品统一管理
- ✅ 需要名称映射和智能聚合
- ✅ 需要查看最新更新

---

## 🎯 典型工作流程

### 场景：翻译多源漫画最新章节

```
1. 点击"高级文件夹"按钮
   ↓
2. 设置根目录：D:\漫画\downloads
   ↓
3. 点击"扫描作品"
   ↓
4. 查看扫描结果：
   ▼ 作品A
     ▼ Manhwa18.cc (KO)    Chapter 14 / 1章  ← 最新，已展开
       ☐ Chapter 14
     ▶ Manga18fx (EN)      Chapter 13 / 2章
     ▶ Manga18.Club (EN)   Chapter 11 / 6章
   ↓
5. 勾选所有作品的最新章节
   ↓
6. 点击"确定"
   ↓
7. 章节添加到文件列表
   ↓
8. 点击"开始翻译"
```

**优势：**
- ✅ 自动找到最新章节
- ✅ 批量处理多个作品
- ✅ 无需手动查找

---

## 🔧 技术细节

### 按钮事件流程

```
用户点击"高级文件夹"按钮
    ↓
main_view.py - advanced_folder_button.clicked
    ↓
app_logic.py - open_advanced_folder()
    ↓
widgets/advanced_folder_panel.py - show_advanced_folder_dialog()
    ↓
显示高级文件夹对话框
    ↓
用户选择章节
    ↓
返回选中的章节路径列表
    ↓
app_logic.py - add_files(selected_chapters)
    ↓
章节添加到文件列表
```

### 核心代码

**对话框入口函数：**
```python
def show_advanced_folder_dialog(parent=None, start_dir: str = "") -> Optional[List[str]]:
    """显示高级文件夹对话框并返回选中的章节路径"""
    dialog = AdvancedFolderDialog(parent, start_dir)
    
    if dialog.exec() == QDialog.DialogCode.Accepted:
        return dialog.get_selected_chapters()
    
    return None
```

**对话框主类：**
```python
class AdvancedFolderDialog(QDialog):
    """高级文件夹选择对话框 - 现代化UI风格"""
    
    def __init__(self, parent=None, start_dir: str = ""):
        super().__init__(parent)
        # 初始化UI
        # 加载缓存
        # 应用样式
```

---

## ✅ 验证方法

### 测试1：按钮显示

```
操作：
1. 启动应用
2. 查看主界面左侧

预期：
✅ 看到4个按钮排成一行：
   [添加文件] [添加文件夹] [高级文件夹] [清空列表]

实际：通过 ✅
```

### 测试2：按钮功能

```
操作：
1. 点击"高级文件夹"按钮

预期：
✅ 弹出高级文件夹对话框
✅ 对话框标题："高级文件夹 - 批量选择章节"
✅ 显示扫描界面

实际：通过 ✅
```

### 测试3：选择章节

```
操作：
1. 点击"高级文件夹"
2. 设置根目录并扫描
3. 勾选章节
4. 点击"确定"

预期：
✅ 对话框关闭
✅ 选中的章节添加到文件列表
✅ 可以看到章节路径

实际：通过 ✅
```

### 测试4：记住路径

```
操作：
1. 使用高级文件夹选择章节
2. 关闭应用
3. 重新打开应用
4. 再次点击"高级文件夹"

预期：
✅ 自动打开上次的根目录
✅ 自动加载上次的扫描结果（如果根目录相同）

实际：通过 ✅
```

---

## 🎉 总结

**完成的修改：**
1. ✅ 在主界面添加"高级文件夹"按钮
2. ✅ 连接按钮点击信号
3. ✅ 实现对话框调用逻辑
4. ✅ 集成到现有文件列表

**按钮位置：**
```
添加文件 | 添加文件夹 | 高级文件夹 | 清空列表
```

**功能特点：**
- ✅ 三层目录智能扫描
- ✅ 多源作品聚合
- ✅ 最新话智能排序
- ✅ 批量选择章节
- ✅ 扫描结果缓存
- ✅ 名称映射同步

现在可以通过主界面上的"高级文件夹"按钮，享受强大的批量章节管理功能了！🚀
