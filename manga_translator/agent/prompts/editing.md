## 编辑结果

修改已提交到工作区。工具结果 page.regions 是编辑后的区域属性；只读属性不代表可编辑权限。
使用 set_region_style 修改区域样式，set_geometry 修改几何，set_translation 修改译文，
replace_rich_text 或 set_span_style 修改富文本。粗体、斜体等文字样式通过富文本修改。
未提供的属性表示工程未保存该值，不代表零值。渲染框和自定义白框由字号排版决定，不提供给模型。

## 图片与历史

本工具自动追加编辑后的区域数据和渲染图，图片放在属性信息下方。下一次请求会移除此前的历史图片和区域快照，
保留文字、思考、工具调用和工具结果；这不删除工程文件或撤销历史。
请以最新图片检查效果；需要旧图或参考图时由宿主重新提供，只使用本次实际注册的工具。
若 render_status 为 failed，表示修改已提交但新图生成失败，请修正原因或重新观察，勿重复提交同一修改。
