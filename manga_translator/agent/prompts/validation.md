本次工具调用未执行。根据全部错误修正参数后，重新调用 tool 字段指定的工具。

- errors 保留原始校验信息，并补充实际字段路径、输入类型、字段要求和错误原因。
- decoded_value 只演示解除多余 JSON 编码后的字段值，不代表该值已经通过校验。
- retry_example 若存在，是保留原值、仅修正 JSON 编码后通过完整参数校验的调用示例；尚未执行，也未检查工作区权限或冲突。
- errors_after_decoding 若存在，表示解除错误编码后仍有字段不合法，必须一起修正；此时不会提供完整的 retry_example。
- 对象和数组直接作为 JSON 结构传入，不要再编码成字符串。不要修改原本就要求字符串的译文内容。
- 同一修改沿用合法的 command_id；若 command_id 本身校验失败，则修正该字段。
- 参数格式和类型错误自行修正，不要求用户解释编程类型或重新授予页面权限。
